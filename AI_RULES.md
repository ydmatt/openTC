# AI_RULES — openTC

## 1. 范围

- 第一版范围以 `docs/REQUIREMENTS.md` 为准。
- 不得擅自加入 FTP、压缩包虚拟目录、插件、全局系统热键或默认文件管理器注册。
- 需求、架构、进度、Bug 和任务只写入本项目，不写入个人长期记忆仓库。

## 2. 安全

- 文件操作必须通过可替换的 `IFileOperationService` 抽象。
- 自动化测试只能操作测试创建的临时目录，不得使用真实项目盘、素材盘、O:、T: 或用户文档目录。
- 删除测试必须验证目标绝对路径位于测试沙箱内。
- 默认删除进入回收站；永久删除必须由显式命令触发并二次确认。
- 自定义右键命令默认使用“可执行文件 + 参数列表”模型，不默认通过 `cmd /c` 拼接任意字符串。
- 不得把用户真实路径、文件名、工作区配置或日志提交到公共仓库。

## 3. 架构

- UI 不直接调用 `System.IO` 或 Win32/COM 文件操作。
- Windows Shell 互操作集中在 Infrastructure 层。
- `IFileOperation` 必须在专用 STA 线程执行。
- 配置文件必须包含 `schemaVersion`，并支持向前迁移。
- 配置保存采用临时文件写入、校验、原子替换和上一版备份。
- `FileSystemWatcher` 事件只用于触发刷新，不作为目录状态唯一真相。

## 4. 交互

- 任何时刻都必须能看出活动窗格和目标窗格。
- Windows 剪贴板命令与 TC 目标窗格命令必须保持不同语义。
- 固定目录标签可临时离开固定目录，但再次点击或激活时必须回到固定目录。
- 自定义标题不得随当前路径变化；真实路径始终显示在地址栏。
- 键盘操作和鼠标操作必须作用于同一命令系统。

## 5. 验证与交接

- 每个功能先定义验收测试，再实现。
- 文件操作、状态恢复、快捷键冲突和路径不可用必须有自动化测试。
- 开发结束前更新 `docs/TASKS.md`、`docs/CHANGELOG.md`、`docs/HANDOVER.md` 和当天会话日志。
- 不得仅凭聊天记录判断项目状态。

## 6. v1.0.0 生产版授权更新（2026-07-28）

- 用户已明确授权增加“普通文件夹/磁盘默认打开程序”和 Win+E；这项新授权取代第一版“不注册默认文件管理器/不使用系统级快捷键”的范围限制。
- 只能做当前用户级、可逆接管：禁止修改 Winlogon Shell，禁止替换或结束 `explorer.exe`，禁止接管桌面和任务栏。
- 接管工具必须在写入前备份原注册表值，并提供不依赖主程序的恢复入口。
- Win+E 桥接必须保持回调快速；桥接未运行时应自然回退到 Windows Explorer。
- 接管和就地升级只允许本机固定磁盘安装目录；不得从 T 盘、UNC 或映射网络盘注册或升级。
- 升级包不得包含 `data`；必须验证产品、版本、架构、路径边界和全部受管文件 SHA-256，并在失败时回滚。
