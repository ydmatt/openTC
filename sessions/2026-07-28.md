# 2026-07-28

## 用户试用反馈

- 快捷键修改不应依赖纯文字输入，应在录入窗口中直接按键或组合键。
- 同一个功能要能设置多个快捷键。
- 移到回收站默认增加 `Del`，永久删除默认增加 `Shift+Del`。
- 快捷键设置需要恢复默认和命名方案保存/载入。

## v0.1.1 实现

- 新增直接按键录入窗口，普通单键或组合键按下后立即完成。
- 连续按键通过录入窗口的连续模式依次捕获。
- 快捷键设置改为一条绑定一行，同一功能可增加多行。
- 保留文字编辑高级入口。
- 新增恢复默认、保存命名方案和载入方案。
- 配置结构升级到版本 2，兼容旧版配置并安全补充删除键默认绑定。
- 增加配置迁移、命名方案、多绑定、按键格式化和 WPF 窗口渲染测试。

## 验证与交付

- Release 全量自动化测试 26/26 通过。
- STA 测试真实向快捷键录入窗口发送 `Delete` 键，确认捕获结果为 `Del`。
- 发布 `v0.1.1` win-x64 自包含绿色包，文件版本为 `0.1.1.0`。
- ZIP 中不含 `data/`；从 ZIP 解压后的独立副本已成功启动并创建 MYTC 主窗口。

## 第二轮试用反馈

- 要求文件和文件夹调用 Windows 系统默认图标。
- 顶部 F5/F6 等按钮不需要常驻，或者应作为可关闭工具栏。
- 右键菜单需要“复制完整路径”。
- 窗口顶部应保留传统“文件、编辑、选项、信息”等菜单。

## v0.1.2 实现

- 文件列表使用 Windows Shell 默认关联图标。
- 新增传统菜单栏；操作按钮工具栏默认隐藏，可在“查看”菜单切换并持久化。
- 右键菜单新增复制完整路径，支持文件、文件夹、多选及空白处当前目录。
- 旧右键菜单配置自动升级。
- Release 自动化测试增加到 29 项，STA 测试实际检查系统图标、菜单、工具栏设置和剪贴板路径。
- `v0.1.2` 自包含 ZIP 不含 `data/`，从 ZIP 解压后的副本已成功创建主窗口，文件版本为 `0.1.2.0`。

## 第三轮试用反馈

- `Del` 在文件列表中无效，而 `Shift+Del` 有效。
- 右键菜单设置需要恢复默认和保存方案。
- 同目录拖放误弹同名冲突。
- 需要 Windows 风格右键拖放并在目标位置创建快捷方式。
- 右键菜单文字需要支持 `&字母` 访问键。
- 此后每次发布还要把可直接运行的文件夹同步到 `T:\mytc`。

## v0.1.3 实现

- 主窗口快捷键改用预览按键事件，修复 DataGrid 消费 `Del`。
- 增加相同目录拖放保护。
- 增加右键拖放动作菜单和 Windows Shell `.lnk` 创建。
- 右键菜单支持 `&字母`，并默认增加“新建文件夹（&W）”。
- 右键菜单设置增加恢复默认和命名方案。
- 全量测试 34/34 通过。
- `v0.1.3` ZIP 解压副本启动成功；完整发布目录已同步到 `T:\mytc`，并从该路径成功启动。
- 用户确认 `T:` 为非本地路径；自动启动触发了 Windows“打开文件 - 安全警告”。后续不再自动启动 T 盘程序，只做静态版本和数据完整性验证。
- 用户确认后续覆盖 `T:\mytc` 前如旧版仍在运行，必须先通知其关闭；不得强制关闭后直接覆盖。

## 第四轮试用反馈

- 文件夹快捷方式应在当前 MYTC 窗格导航。
- 标签栏空白双击新建；中键关闭标签；标签右键菜单提供“固定当前目录”，单纯右键只打开菜单。
- 同目录 `Ctrl+C`、`Ctrl+V` 应创建备份副本。
- `Del` 第一次无效，需要修复。
- 增加 `Alt+D` 聚焦并全选活动窗格地址栏。

## v0.1.4 实现

- 使用 Windows Shell 解析 `.lnk`，文件夹目标在当前窗格导航。
- 增加三种标签快捷交互。
- 同目录复制粘贴使用 KeepBoth 生成 `(2)` 副本。
- 快捷键处理前同步活动文件列表选择，单次 `Del` 测试通过。
- 快捷键配置升级到版本 3并默认增加 `Alt+D`。
- 全量测试 35/35 通过。

## v0.1.4 最终交付

- 生成 win-x64 自包含便携目录和 ZIP，发布包不含 `data/`。
- ZIP 已独立解压并在本地启动验证，程序文件版本为 0.1.4.0。
- ZIP SHA-256 为 `61E45EDAFCA418241A3E45CD8259A0D140104023F41599BF60F615F6B4956BE1`。
- 覆盖部署到 `T:\mytc` 前确认没有旧版进程运行。
- 部署后静态确认 `MYTC.exe` 为 0.1.4.0、程序文件 408 个；用户 `data` 覆盖前后均为 7 个文件、22,458 字节。
- 一次从 `T:` 启动的验证触发了 Windows 非本地位置安全警告。没有修改或绕过系统安全设置；后续不再由 Codex 自动启动 T 盘程序，只做静态部署校验。

## 第五轮试用反馈

- 标签栏空白处双击没有新增标签。
- 删除确认第一次可触发，但取消后下一次 `Del` 仍需按两次。
- 任务栏右键缺少“固定到任务栏”。
- `Alt+D` 后 `Ctrl+V` 偶尔无法粘贴路径。

## v0.1.5 实现与验证

- 标签栏改用预览鼠标双击路由，避免内部 ScrollViewer 吞掉事件。
- 删除确认取消或完成后恢复活动文件列表焦点并重置快捷键序列。
- 地址栏接管标准粘贴命令，兼容普通文本路径和 Windows 文件剪贴板路径。
- 为进程和主窗口设置 AppUserModelID、重新启动命令、显示名称和图标，满足 Windows 任务栏固定身份要求。
- 针对性 WPF 测试验证真实标签栏双击、连续两次取消删除时单次 `Del`、文本/文件路径粘贴及任务栏属性写入。
- Release 全量自动化测试 35/35 通过。

## v0.1.5 最终交付

- 生成 win-x64 自包含便携目录和 ZIP，文件版本为 0.1.5.0，ZIP 含 408 个文件且不含 `data/`。
- 从 ZIP 解压到全新本地目录，使用独立测试配置成功创建 MYTC 主窗口。
- ZIP SHA-256 为 `A4FA13AD1D96BF238D52DBC87709662B420E3A614872C47BF8D84CA1111D648E`。
- 覆盖 `T:\mytc` 前再次确认没有从该目录运行的 MYTC 进程。
- 覆盖后静态确认 `T:\mytc\MYTC.exe` 为 0.1.5.0、程序文件 408 个且不含 ZIP。
- 用户 `data` 覆盖前后均为 8 个文件、28,517 字节，逐文件哈希形成的汇总指纹保持 `8DBC3E09C03B1E2A5EB35E3DA9E099221E9C5FAFF300A7E14D0A890466984E80`，配置内容未变化。
- 未从 `T:` 自动启动程序，避免再次触发非本地位置安全警告。

## 第六轮试用反馈

- 删除确认后下一次 `Del` 仍可能需要按两次。
- `Alt+D` 有时激活顶部菜单，导致随后 `Ctrl+V` 失效。
- 文件列表增加 `Backspace` 上级目录，`Ctrl+D` 等同 `Del` / `F8`。
- 文件右键菜单增加 Windows“打开方式（H）”。
- 标签右键菜单增加完整复制当前标签到目标窗格。
- 标签右键菜单需要像文件右键菜单一样可排序、恢复默认并保存方案。
- 程序、标题栏和任务栏使用清晰的 `TC` 图标。

## v0.1.6 实现与验证

- 删除键增加物理释放门控，并接受确认框后下一次被 WPF 标记为重复的首次事件；STA 测试把第二次 `Del` 设置为 `IsRepeat=true` 后仍一次触发。
- `Alt+D` 拦截 Alt 松开时的菜单激活并再次聚焦地址栏。
- 快捷键配置升级到版本 4，默认增加 `Backspace` 和 `Ctrl+D`；迁移时保护现有连续按键前缀。
- 文件右键菜单配置升级到版本 4并增加 `SHOpenWithDialog`；仅单选文件时启用。
- 完整实现标签跨窗格复制及独立标签右键菜单设置、恢复默认和命名方案。
- 使用内置 ImageGen 生成 `TC` 源图，转换为九种尺寸 ICO，并嵌入 EXE、窗口和任务栏身份。
- Release 全量自动化测试 37/37 通过；ZIP 解压副本创建主窗口、响应和正常关闭保存会话均通过。

## v0.1.6 最终交付

- 自包含便携目录和最终 ZIP 均含 408 个文件且不含 `data/`；文件版本为 0.1.6.0。
- 最终 ZIP SHA-256 为 `EBDD71E3A1CEBC1BF790195D68E26EAEBAA34585794619ABB0F10340563E24C7`。
- 覆盖 `T:\mytc` 前确认没有旧版进程运行；目标 EXE 与发布源 SHA-256 一致。
- `T:\mytc\data` 覆盖前后均为 9 个文件、30,669 字节，逐文件路径、长度和 SHA-256 全部一致。
- 未从 `T:` 自动启动程序，未触发或绕过非本地位置安全警告。

## v1.0.0 生产版需求

- 用户要求 MYTC 可作为普通文件夹和磁盘的默认打开程序，并支持 Win+E。
- 程序将固定安装到本机目录（例如 `E:\port\MYTC`），需要独立注册工具及后悔后的恢复工具。
- 后续升级包放在 T 盘固定目录，由 MYTC 菜单选择 ZIP 后自动退出、替换、保留配置并重启，避免跨设备覆盖运行中 EXE。

## v1.0.0 实现与验证

- 新增当前用户级 Shell 注册、原值备份与恢复；不修改 Explorer/Winlogon Shell。
- 新增独立 Win+E 低级键盘桥接，登录启动项由注册工具管理。
- 新增主程序单实例和当前用户命名管道，外部目录请求转发到活动窗格。
- 新增 `MYTC.Maintenance.exe` 单文件维护程序，包含注册/恢复、Win+E 桥接、更新和清理模式。
- 新增升级 ZIP 产品/版本/架构、路径边界、额外文件、`data` 禁止项和逐文件 SHA-256 校验。
- 新增旧版备份、失败逆序回滚、日志、`data` 保留、自动重启、暂存和更新器宿主清理。
- Release 自动化测试 48/48 通过。
- 发布版主程序黑盒测试：主窗口出现、第二实例目录请求转交、两进程均正常退出、会话保存转发目录。
- 更新器黑盒模拟 `1.0.0 → 1.0.1`：返回 0，目标版本、更新内容、备份、日志和 `data` 保留全部通过。
- 独立维护窗口显示/关闭、Win+E 钩子启动/恢复信号退出、清理宿主均返回 0。
- 最终 ZIP SHA-256 为 `7717507957FDF392AD03DA40A9DCAF343B13BEBA8CCB238983FD4A545FAF7BB7`。
- ZIP 已同步到 `T:\mytc-updates`；完整目录已同步到 `T:\mytc-releases\MYTC-v1.0.0-win-x64`，未覆盖旧 `T:\mytc`。
- T 盘 ZIP 哈希与本地一致；清单版本 1.0.0，406 个受管文件逐项长度和 SHA-256 正确，`data` 条目为 0。
