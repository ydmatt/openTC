# PROJECT_MEMORY

## 当前状态

- 阶段：第一版修订版 `v0.1.7` 已完成开发、测试、打包和 `T:\mytc` 覆盖部署，可交付试用
- 日期：2026-07-28
- 技术栈：C# + WPF + .NET 10 LTS
- Release 自动化测试 40/40 通过
- STA WPF 界面冒烟测试及真实“回收站删除 → 撤销还原”集成测试通过
- win-x64 自包含发布在 Windows 10 实机成功启动

## 用户原始目标

用户希望制作自己的资源管理器，参考 Total Commander 和所提供的 Q-Dir 四窗格截图。第一版重点是：

- 默认四个小窗口（初步理解为 2×2 四窗格）
- 每个窗格支持多标签
- 标签可以自定义显示标题
- 普通标签保持在最后一次操作的位置
- 标签可以启用“固定目录”模式；启用后，每次点击或重新激活标签都进入预设的固定目录
- 支持自定义快捷键

## 2026-07-27 已确认决策

- 核心场景一是标书工作，在项目、素材、临时文件、AI 生成文件等目录间频繁打开、复制和移动。
- 核心场景二是 YouTube 视频制作，在工具、视频、图片、项目等目录间切换。
- 第一版最重要的差异是四个目录同时可见。
- 支持 Windows 10/11，仅供用户本人使用。
- 第一版作为 Windows 资源管理器的辅助工具；成熟后可能研究成为系统默认文件管理器。
- 第一版固定以 2×2 四窗格为主，架构上保留未来自定义及保存布局的可能。
- 四窗格相互独立，分隔线可调整，活动窗格可以临时最大化并恢复。
- 复制移动采用“活动窗格为源、最近使用的其他窗格为目标”的基础模型，并需要清晰标识源与目标。
- 每个窗格带独立地址栏。
- 自定义标签标题始终保持，不随当前目录变化。
- 标签按建议支持新建、关闭、重命名、排序、固定和误关闭恢复。
- 启动时恢复上次窗格、标签、当前目录和布局。
- 支持保存工作区布局方案，并通过加载不同设置切换工作区。
- 第一版提供 Windows 资源管理器级别的基本文件操作。
- 必须支持系统默认程序打开、窗格间拖放、与外部程序互相拖放、系统剪贴板。
- 路径范围采用建议基线：本地磁盘、移动设备和普通网络路径；第一版不把压缩包和 FTP 当作目录浏览。
- 在同一默认方案中合并 Windows 与 Total Commander 两套操作习惯：Windows 剪贴板命令与 TC 直接操作目标窗格命令保持为不同命令；同一命令仍允许多个快捷键，再允许逐项修改。
- 快捷键仅在 MYTC 内生效；支持单键、组合键及连续按键序列；第一版不要求导入导出。
- 软件必须是免安装绿色便携版。
- 常用操作集中在左上与右上、右上与右下窗格之间。
- 右下窗格通常放 T:、O: 等固定工作盘。
- 单个目录通常少于 100 个对象。
- 每个窗格必须有盘符下拉菜单，用于快速切换 C:、D:、O: 等盘符。
- 文件列表必须提供名称、修改日期、类型、大小等列，并支持点击列标题排序。
- 右键菜单需要支持自定义。
- 安全策略已确认：普通删除进入回收站；永久删除二次确认；重名支持覆盖、跳过、自动重命名；批量操作显示进度和失败清单；第一版不实现完整撤销系统。
- 自定义右键菜单已确认：支持内置项目的显示、隐藏与排序，以及添加带当前文件、选中文件、当前目录等变量的外部程序命令；完整 Windows Shell 菜单不是第一版硬性要求。
- 工作区保存范围已确认：保存窗格布局与比例、各窗格标签、标签顺序和标题、普通标签最后目录、固定目录及路径、排序状态、活动窗格与目标窗格。
- 快捷键、右键菜单和主题是全局设置，不随工作区切换。

## 2026-07-27 实现与验证进展

- 已使用 C# + WPF + .NET 10 LTS 完成正式应用。
- 已实现 win-x64 自包含绿色目录发布，不要求系统预装 .NET。
- 已建立 Presentation、Application、Domain、Infrastructure 分层。
- 文件操作采用后台托管服务；普通删除通过 Windows 集成进入回收站。
- 已实现工作区、窗格、标签、快捷键、右键菜单和文件操作数据模型及 JSON 持久化。
- 已实现默认 2×2 四窗格、独立地址栏、盘符菜单、详细信息列和排序。
- 已实现普通/固定目录标签、多标签管理、会话恢复和命名工作区。
- 已实现 F5/F6 目标窗格操作、剪贴板、拖放、冲突处理、进度、取消和失败清单。
- 已实现单键、组合键、连续按键编辑器；默认 Windows 与 TC 预设合并。
- 已实现可隐藏、改名、排序和添加外部程序的右键菜单设置。
- 已制作 `prototype/index.html` 四窗格交互原型。
- 已使用本机 Edge 自动验证四窗格、活动/目标窗格、固定目录标签复位、列排序、连续快捷键、工作区切换、最大化和右键菜单。
- 验证过程中发现并修复了非活动窗格点击触发重绘、吞掉标签或排序动作的问题。
- 已校验用户提供的 .NET SDK 压缩包，并安装到项目忽略目录 `.tools/dotnet/`。
- 正式应用首次框架依赖启动暴露运行时缺失，已改用自包含绿色发布并验证成功。
- Computer Use 在当前 Windows 10 捕获 WPF 窗口时返回 `SetIsBorderRequired 0x80004002`；改用 STA 应用内界面渲染测试，成功生成四窗格截图。
- 文件操作自动化均使用唯一临时沙箱；回收站集成另用唯一临时文件探针验证。

## 2026-07-28 试用反馈与修订

- 用户指出纯文字修改快捷键不直观，尤其不知道 `Del` 等按键应输入什么。
- `v0.1.1` 改为双击快捷键行或点击“录入/修改”后，直接按单键或组合键完成录入；连续按键可在录入窗口启用后逐段按下。
- 保留“文字编辑”作为高级入口，但不再是默认修改方式。
- 同一功能支持多条快捷键，以多行显示，并可逐条增加、修改或删除。
- 默认“移到回收站”同时绑定 `Del`、`F8`；“永久删除”同时绑定 `Shift+Del`、`Shift+F8`。
- 快捷键设置增加“恢复默认”、命名方案保存和方案载入。
- 旧版快捷键配置自动升级；若 `Del` 或 `Shift+Del` 已被用户用于其他功能，不会强行覆盖。
- STA WPF 冒烟测试增加快捷键设置窗口和按键录入窗口的真实 XAML 渲染验证。
- Release 全量自动化测试 26/26 通过，并由测试真实向录入窗口发送 `Delete` 键，确认自动保存为 `Del`。
- `MYTC-v0.1.1-win-x64.zip` 已从全新目录发布，确认不包含 `data/`，并从 ZIP 解压后的副本成功创建主窗口。

## 2026-07-28 第二轮试用反馈与修订

- 文件和文件夹图标改为调用 Windows Shell 默认关联图标；普通文件按扩展名复用系统图标，程序、快捷方式和图标文件读取自身图标。
- 窗口顶部增加传统“文件、编辑、查看、选项、信息”菜单栏。
- 原“复制到目标 F5”等按钮归为操作工具栏，新安装默认隐藏，可从“查看 → 操作工具栏”显示或关闭，状态保存到 `data/ui-preferences.json`。
- 自定义右键菜单新增内置“复制完整路径”：文件复制文件绝对路径，文件夹复制文件夹绝对路径，多选时每行一条；在文件列表空白处右键则复制当前目录。
- 旧版右键菜单配置自动升级并加入“复制完整路径”，不覆盖已有自定义顺序与项目。
- STA WPF 测试实际验证 Shell 图标、传统菜单、工具栏显示状态保存，以及剪贴板中的完整路径。
- `MYTC-v0.1.2-win-x64.zip` 已从全新发布目录生成，不含 `data/`，并从 ZIP 解压副本成功创建主窗口；文件版本为 `0.1.2.0`。

## 2026-07-28 第三轮试用反馈与修订

- 修复焦点在文件列表时 `Del` 被 DataGrid 消费、无法触发移到回收站的问题；主窗口改用预览按键处理，`Shift+Del` 行为保持不变。
- 修复在同一目录内拖住文件后释放会误触发“目标中存在同名项目”的问题；源项目父目录全部等于目标目录时直接判定为无操作。
- 增加右键跨窗格拖放：释放后弹出“复制到此处、移动到此处、在当前位置创建快捷方式、取消”菜单。
- “在当前位置创建快捷方式”通过 Windows Shell 创建真正的 `.lnk`，同名时自动生成“(2)”等可用名称。
- 右键菜单文字支持 Windows 风格 `&字母` 访问键；例如 `复制完整路径（&A）` 在实际菜单显示为带下划线的 A，并可直接按 A 执行。
- 默认右键菜单增加“新建文件夹（&W）”，打开菜单后可以直接按 W。
- 右键菜单设置增加恢复默认、保存命名方案和载入方案，方案保存在 `data/context-menu-schemes/`。
- 右键菜单配置结构升级到版本 3；旧配置自动补充复制完整路径和新建文件夹。
- Release 自动化测试 34/34 通过，包含 DataGrid 焦点下的 Delete 预览路由、同目录拖放保护、真实 `.lnk` 创建、访问键转换和右键菜单方案。
- `MYTC-v0.1.3-win-x64.zip` 不含 `data/`，从 ZIP 解压副本启动成功；同一发布目录已复制到 `T:\mytc`，文件版本 `0.1.3.0`，不含 ZIP，并从该路径成功创建主窗口。

## 2026-07-28 第四轮试用反馈与修订

- 文件夹 `.lnk` 快捷方式不再交给 Windows 资源管理器；通过 Windows Shell 解析目标后，直接在当前 MYTC 窗格导航。普通文件快捷方式仍由系统默认行为打开。
- 标签栏空白处双击执行新建标签。
- 鼠标中键点击标签执行关闭标签，并保持至少一个标签。
- 标签右键菜单增加“固定当前目录”：单纯右键只弹出菜单，用户选择该菜单项后才执行固定；无需再弹设置窗口，固定路径取该标签当前目录，自定义标题取当前目录名称。“标签设置…”继续保留。
- 当前目录内 `Ctrl+C`、`Ctrl+V` 使用“保留两者”创建副本，例如 `note.txt` 生成 `note (2).txt`；同目录剪切粘贴仍判定为无操作。
- 主窗口在处理快捷键前同步当前文件列表的实际选择和活动窗格，修复第一次 `Del` 未取到选择、需要按第二次的问题。
- 快捷键配置结构升级到版本 3，默认增加 `Alt+D`“激活当前地址栏”；执行后焦点进入活动窗格地址栏并全选路径文字。
- 旧快捷键配置自动补充 `Alt+D`；如果该组合已被用户绑定到其他操作，则不抢占。
- Release 自动化测试 35/35 通过，包含真实 `.lnk` 解析、同目录副本、单次 Delete 路由和地址栏聚焦全选。
- ZIP 已完成独立解压和本地启动验证，SHA-256 校验文件已生成。
- 已覆盖部署到 `T:\mytc`：`MYTC.exe` 版本为 0.1.4.0，程序文件 408 个；用户 `data` 目录覆盖前后均为 7 个文件、22,458 字节，未被覆盖。

## 2026-07-28 第五轮试用反馈与修订

- 标签栏双击改用预览鼠标事件，确保 ScrollViewer 等内部区域不会吞掉空白处双击；WPF 测试向真实标签栏滚动区域发送双击并确认新增标签。
- 删除确认框无论取消还是执行结束，都会把键盘焦点还给活动文件列表并重置连续按键状态；WPF 测试连续两次取消确认，两次单按 `Del` 都立即触发。
- 地址栏接管 WPF 标准粘贴命令，支持普通 Unicode 文本路径，也支持 Windows 文件剪贴板中的第一个文件或文件夹路径；两种来源均由 WPF 测试验证。
- 启动时为 MYTC 进程设置 `AIDELL.MYTC.FileManager` AppUserModelID，并为主窗口设置重新启动命令、显示名称和图标，使 Windows Shell 能生成“固定到任务栏”入口。
- “固定到任务栏”最终菜单由 Windows Shell 和系统策略控制；不得绕过系统禁止固定的策略。`T:` 的非本地位置安全警告不因固定而被绕过。
- 版本升级到 `v0.1.5`，Release 全量自动化测试 35/35 通过。
- `MYTC-v0.1.5-win-x64.zip` 含 408 个文件、不含 `data/`，从 ZIP 独立解压的 0.1.5.0 副本成功创建主窗口。
- v0.1.5 已覆盖部署到 `T:\mytc`；覆盖前后用户 `data` 均为 8 个文件、28,517 字节，逐文件内容指纹汇总保持一致。

## 2026-07-28 第六轮试用反馈与修订

- `Delete` 快捷键增加物理按键释放门控：确认框关闭后主动等待系统报告 `Delete` 已释放，同时允许下一次首次事件即使被 WPF 标记为重复事件也正常执行；STA 测试把第二次 `Del` 明确标记为 `IsRepeat=true`，两次均一次触发。
- `Alt+D` 在按下时聚焦并全选活动地址栏，Alt 松开时由主窗口拦截菜单激活并再次恢复地址栏焦点，避免顶部“文件/编辑”菜单抢走后续 `Ctrl+V`。
- 默认增加 `Backspace`“上级目录”和 `Ctrl+D`“移到回收站”。快捷键配置升级到版本 4；旧配置遇到 `Ctrl+D, P` 等连续按键前缀时不会强行加入 `Ctrl+D`。
- 文件右键菜单配置升级到版本 4，增量增加 Windows 原生“打开方式（&H）”；仅单选文件时启用，通过 `SHOpenWithDialog` 调用。
- 标签右键菜单增加“复制标签到目标窗格”，复制自定义标题、普通/固定模式、当前/固定目录、前进/后退历史及排序状态。
- 增加独立标签右键菜单配置和 `data/tab-context-menu.json`：支持显示/隐藏、改名、排序、分隔线、恢复默认，以及在 `data/tab-context-menu-schemes/` 保存和载入命名方案。
- 使用内置图像生成工具制作 `TC` 图标源图，转换为 16/20/24/32/40/48/64/128/256 多尺寸 ICO，并用于 EXE、窗口标题栏和任务栏；32×32 小尺寸已人工检查可辨识。
- Release 全量自动化测试 37/37 通过；Computer Use 仍因当前系统 WPF 捕获接口返回 `0x80004002` 而无法注入实机按键，按插件恢复规则停止后，以 STA WPF 真实 XAML 测试和独立进程启动测试完成验收。
- `MYTC-v0.1.6-win-x64.zip` 含 408 个文件、不含 `data/`；SHA-256 为 `EBDD71E3A1CEBC1BF790195D68E26EAEBAA34585794619ABB0F10340563E24C7`。解压副本 `MYTC.exe` 版本 0.1.6.0，成功创建主窗口、响应正常并正常退出保存会话。
- 覆盖 `T:\mytc` 前确认没有从该目录运行的 MYTC。首次目录级复制被 PowerShell 拒绝且未改变目标，随后改用遇错即停的逐文件覆盖，成功同步 408 个程序文件；目标 EXE 与发布源哈希一致。用户 `data` 覆盖前后均为 9 个文件、30,669 字节，逐文件路径、长度和 SHA-256 全部一致。

## 2026-07-28 第七轮试用反馈与修订

- 文件右键菜单改为 Windows 风格两级“新建（&W）→ 文件夹（&F）”；配置升级到版本 5，旧版默认项目自动迁移且保留自定义项目。
- 右键菜单打开期间停用输入法组合，菜单动作在菜单关闭后的空闲队列执行，修复中文输入法下快速按 `W`、`F` 导致程序重入卡死的问题。
- “选项 → 全局设置”增加当前用户开机启动和普通回收站删除确认开关；永久删除仍始终确认。界面偏好配置升级到版本 2。
- 文件列表的 `Enter` 改为打开所选项目，文件夹直接在当前窗格进入，不再让 DataGrid 移动到下一行。
- 文件右键菜单增加“撤销删除”，安全匹配 Windows 回收站中的原目录、真实文件名和删除时间，还原当前 MYTC 会话内最近一批删除。
- 标签支持直接拖到另一个窗格复制，源标签保留，复制内容与“复制标签到目标窗格”一致。
- Release 全量自动化测试 40/40 通过，包含 v0.1.6 菜单/界面偏好配置迁移、STA WPF 两级菜单与全局设置、`Enter` 导航，以及真实临时文件“回收站删除 → 撤销还原”。
- `MYTC-v0.1.7-win-x64.zip` 含 408 个文件、不含 `data/`，SHA-256 为 `09EA141F4E09A770AC72667416D92A070335559B61F7BDEF85A7A518C5CF3F8C`；独立解压副本版本 0.1.7.0，成功创建窗口、保持响应并正常退出。
- 覆盖 `T:\mytc` 时发现另一台设备上有多个挂起 MYTC 进程，SMB 远端锁无法由本机进程列表或 Restart Manager 识别；用户结束其中部分进程后锁释放。最终 408 个程序文件与发布源逐文件 SHA-256 一致，目标 EXE 为 0.1.7.0。
- 发布源始终不含 `data`。覆盖前后用户配置均为 9 个文件；旧 MYTC 在远端退出期间保存了新的 `session.json` 和工作区，因此内容指纹发生合理变化。覆盖后全部 9 个配置文件均通过 JSON 完整性检查，保留最新会话，没有回退旧状态。

## 固定交付规则

- 从 `v1.0.0` 起，升级 ZIP 固定同步到 `T:\mytc-updates`，供程序内“从升级包升级”选择。
- 可直接复制安装的完整生产目录按版本同步到 `T:\mytc-releases\MYTC-vX.Y.Z-win-x64`；不再覆盖旧的 `T:\mytc` 根目录。
- 用户把首个生产版手动复制到本机固定目录（例如 `E:\port\MYTC`），并在关闭旧版后把旧 `data` 复制过去；后续由程序内更新器保留该目录的 `data`。
- 若未来仍需直接覆盖任何已安装目录，覆盖前必须确认没有从该目录运行的 MYTC；如正在运行，停止部署并通知用户先关闭，不得强制结束。
- `T:` 是 SMB 映射盘，程序可能在其他设备运行。本机进程列表与 Restart Manager 无法发现其他设备持有的远端锁；覆盖前除本机进程检查外，还必须验证目标核心文件可写。若被锁定，应询问用户检查所有设备，禁止擅自断开共享盘。
- `T:` 不是本地路径，从 `T:\mytc` 启动会弹出 Windows“打开文件 - 安全警告”。后续发布只能检查复制结果、文件版本和 `data` 完整性，不得由 Codex 自动启动 T 盘程序，以免主动触发可见的风险确认窗口；也不得修改 Windows 安全区域或绕过该提示。实际启动由用户决定。

## 2026-07-28 v1.0.0 生产版扩展

- 用户确认下一版作为生产版，并允许在实现完成后推送 GitHub。
- 采用安全接管而非替换 Explorer Shell：当前用户 `Directory`/`Drive` 默认动词 + 独立 Win+E 桥接；不修改 Winlogon，不结束 Explorer。
- `MYTC.Maintenance.exe` 提供注册与恢复 UI，原注册表值保存在 LocalAppData，即使主程序损坏仍能恢复。
- 程序必须先放到本机固定磁盘（例如 `E:\port\MYTC`）；注册和就地升级拒绝映射盘/UNC。
- 主程序增加单实例和当前用户命名管道，新目录在现有活动窗格打开。
- 升级 ZIP 含产品/版本/架构/逐文件 SHA-256 清单，拒绝路径穿越、额外文件和任何 `data`。
- 更新器从安装目录外运行，等待主程序/桥接退出，备份旧版、逐文件替换、失败回滚、保留 `data`、写日志并重启。
- Release 48/48 测试通过；发布主程序目录转发、维护窗口、Win+E 桥接生命周期及外部更新器 `1.0.0 → 1.0.1` 黑盒测试通过。
- GitHub 推送前仍需确认目标仓库地址与公开/私有属性；项目文档含用户工作流和盘符信息，不能擅自公开。
- `v1.0.0` ZIP 已投递到 `T:\mytc-updates`，完整目录已投递到 `T:\mytc-releases\MYTC-v1.0.0-win-x64`；ZIP SHA-256 为 `7717507957FDF392AD03DA40A9DCAF343B13BEBA8CCB238983FD4A545FAF7BB7`，T 盘 406 个受管文件长度和哈希全部一致。

## 2026-07-28 v1.0.1 生产修订

- 用户指定 GitHub 仓库名 `MyTC`，必须是私有仓库。
- `T:` 是 `DriveType.Network` 的映射共享盘，Windows Shell 回收站不能为该盘提供可靠的撤销记录。MYTC 对映射盘和 UNC 路径改用同一共享根目录下的隐藏 `.MYTC-RecycleBin`：删除使用同卷移动，撤销按记录的原路径移回；原位置重名时拒绝覆盖。文件列表始终隐藏该保留目录。
- 共享盘自管回收目前沿用“当前会话最近一次删除”的产品边界；用户不应手动操作 `.MYTC-RecycleBin`。
- `Backspace` 现在等待上级目录导航完成，再选中第一项并把键盘焦点落到首个 DataGrid 单元格，方向键可以立即继续导航。
- 标签拖动由标签按钮自身 `MouseMove` 改为源窗格级 `PreviewMouseMove`，避免按钮捕获鼠标后拖动事件丢失；拖放数据同时注册自定义格式和强类型格式。
- Win+E 桥接在拦截 `E` 时向系统注入无操作的 `Win+F24` 标记，防止 Windows 键抬起被识别为单独按键而打开开始菜单。
- 第二实例命名管道增加主进程 PID 握手；转发前调用 `AllowSetForegroundWindow`，主窗口再用 `AttachThreadInput`、`BringWindowToTop` 和 `SetForegroundWindow` 可靠置前。
- Release 自动化测试 52/52 通过。发布版主/次实例转发黑盒测试、外部更新器保留 `data`/备份/日志测试、成品 Win+E 钩子安装与退出信号测试全部通过。
- `v1.0.1` 发布包主程序和维护程序文件版本均为 `1.0.1.0`，含 406 个受管文件；本地 ZIP SHA-256 为 `A89632C2F1CAC05B877095F06A0F1987878B082465130ECC79E75106AA7E8D91`。
- ZIP 已投递到 `T:\mytc-updates\MYTC-v1.0.1-win-x64.zip`，完整目录已投递到 `T:\mytc-releases\MYTC-v1.0.1-win-x64`；目标 407 个实际文件逐项长度和 SHA-256 校验零差异，目标 ZIP 哈希一致。
- 已创建并推送 GitHub 私有仓库 `ydmatt/MyTC`，默认分支为 `main`；初始发布提交为 `59c5b77`。仓库忽略 `.tools`、构建产物、发布包及便携版 `data`，提交前敏感凭据模式扫描为零。

## 2026-07-28 v1.0.2 实机反馈修订

- v1.0.1 实机反馈：Win+E 打开前桌面仍会短暂闪暗；Backspace 后看似聚焦第一项，但方向键仍不移动。
- Win+E 根因是 v1.0.1 先把物理 Windows 键按下传给系统，再用 F24 标记组合键，系统界面仍有机会开始响应。v1.0.2 改为先拦截并暂存 Windows 键：若下一键是 E，则整组按下/抬起都不传给系统；若是其他键或单独抬起，则在原事件前注入带专用标记的 Windows 键按下，保持其他系统快捷键。
- Backspace 现在等待目录读取完成，并在 Dispatcher `ContextIdle` 阶段聚焦首项；文件 DataGrid 对无修饰键 `↑`/`↓` 显式计算、选择、滚动和聚焦目标行。
- 自动化测试增加至 56 项；STA WPF 测试在 Backspace 后对真实焦点元素发送 `↓`，强制验证 SelectedIndex 从 0 变成 1，不再只断言焦点外观。
- 发布版主/次实例目录转发、外部更新器 `1.0.2 → 1.0.3`、`data` 保留、旧版备份、日志及 Win+E 钩子安装/退出生命周期验证通过。
- v1.0.2 主程序和维护程序版本均为 `1.0.2.0`；ZIP SHA-256 为 `7E93CF6E2835C5055E5C169CCD97CCE80C3A51F190B7D6307AC938C74BF07467`。
- ZIP 已投递到 `T:\mytc-updates\MYTC-v1.0.2-win-x64.zip`，完整目录已投递到 `T:\mytc-releases\MYTC-v1.0.2-win-x64`；目标 407 个实际文件逐项长度和 SHA-256 校验零差异，目标 ZIP 哈希一致。

## 2026-07-28 v1.0.3 小屏幕窗口修订

- v1.0.2 实机反馈：在分辨率较小或显示缩放较高的系统里，主窗口右上角最小化/最大化按钮不可见。
- 根因是固定初始尺寸 `1440×900` 配合 `CenterScreen`；当当前逻辑工作区小于该尺寸时，WPF 会把窗口居中到屏幕外。原主窗口、左右列和上下行最小尺寸也会阻止继续收缩。
- 新增 `AdaptiveWindowPlacement`：在 `SourceInitialized` 后读取当前显示器物理工作区与 `GetDpiForWindow`，换算逻辑坐标，按工作区四周 8 DIP 边距缩放并手动居中。
- 主窗口设计最小尺寸降为 `640×400`，窗格列最小宽度降为 260、行最小高度降为 110；极小工作区还会临时降低有效窗口最小值，优先保证标题栏和系统按钮可见。
- 自动化测试增加至 61 项，覆盖多组小工作区，并由 STA WPF 冒烟测试验证真实首次显示窗口没有越出系统工作区。
- 发布版主/次实例启动和外部更新器 `1.0.3 → 1.0.4` 黑盒验证通过，用户 `data`、备份和日志均正常。
- v1.0.3 主程序和维护程序版本均为 `1.0.3.0`；ZIP SHA-256 为 `9623028D4D82E8A51A7CA6844DE931007783F3B9614462BA5CFB0D5672E29450`。
- ZIP 已投递到 `T:\mytc-updates\MYTC-v1.0.3-win-x64.zip`，完整目录已投递到 `T:\mytc-releases\MYTC-v1.0.3-win-x64`；目标 407 个实际文件逐项长度和 SHA-256 校验零差异，目标 ZIP 哈希一致。

## 截图观察

截图中的软件是 Q-Dir 11.14，主要可见结构为：

- 2×2 四个文件窗格
- 每个窗格有独立的标签栏、地址栏、文件列表和状态区域
- 各窗格可以同时浏览不同目录
- 文件列表采用详细信息视图，含名称、修改日期、类型、大小等列
- 窗格之间由横向、纵向分隔条划分
- 顶部有全局菜单和布局切换入口

截图仅用于需求参考，原始图片位于用户会话临时目录，未复制到项目仓库。

## 当前推断（未确认）

- “模仿 Total Commander”更偏向操作效率和键盘工作流。
- “参考截图”更偏向 Q-Dir 式四窗格布局。
- 第一版应优先验证四窗格、多标签、标签配置和快捷键配置，不宜一开始复刻完整 Windows 文件资源管理器。

## 已确认的标签目录行为

### 普通标签

- 浏览过程中，标签持续记录当前目录。
- 切换到其他标签或窗格后再回来，仍停留在最后一次操作的位置。
- 软件关闭时保存最后位置，并在下次启动时恢复。

### 固定目录标签

- 用户可以为某个标签设置一个固定目录。
- 每次点击或重新激活该标签时，标签都进入这个固定目录。
- 用户进入固定目录后仍可继续浏览其子目录或其他目录，但切走再回来时会再次回到固定目录。
- 为避免术语混淆，需求文档暂称其为“固定目录标签”；它不等同于“把标签固定在标签栏、禁止关闭”的置顶功能。

### 待补充的边界

- 固定目录被移动、删除或暂时不可访问时的降级行为。
- 固定目录标签是否允许同时具备“禁止关闭/置顶”属性。

## 待确认

- 第一版产品范围无待确认项。
- 连续快捷键的默认等待时长、视觉提示样式等实现参数在交互原型阶段确定，不改变已冻结的功能范围。

## 下一步

1. 用户用标书和视频制作真实场景验证目录切换、F5/F6、标签固定和工作区。
2. 收集问题进入 `v0.1.x` 修复；后续候选包括自动目录监听、Shell 属性、更多布局和配置导入导出。
